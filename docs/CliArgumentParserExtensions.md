# CliArgumentParserExtensions

Provides extension methods for parsing, validating, and summarizing command-line arguments in a structured manner. This class facilitates the conversion of raw command-line input into a strongly-typed `CliOptions` object while handling common scenarios such as help requests, version queries, and validation errors.

## API

### `ParseWithValidation`
