# DataTransformationUtility

The `DataTransformationUtility` class provides a collection of static methods for processing and analyzing health data records. It facilitates aggregation, filtering, normalization, and statistical operations on datasets such as sleep metrics, heart rate readings, and step counts. This utility is designed to transform raw health data into structured, actionable insights for downstream analysis or visualization.

## API

### `AggregateSleepByDate`
